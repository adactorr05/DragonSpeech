package com.dragonspeech.accessory;

/** The 5 material tiers a piece of jewelry can be made from - MIXED is deliberately the best of all (see AccessoryItems for capacities), using all four other materials in its recipe rather than one. */
public enum AccessoryMaterial {
    GOLD,
    EMERALD,
    DIAMOND,
    NETHERITE,
    MIXED
}
